{
  "name": "shipyard-aftercare",
  "version": "0.1.0",
  "description": "Cloudflare Worker for post-ship lifecycle emails (Homeport)",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest",
    "test:run": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240605.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.30.0",
    "vitest": "^1.0.0"
  }
}
