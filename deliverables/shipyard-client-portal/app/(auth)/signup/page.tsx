import { SignupForm } from "@/components/SignupForm";

export const metadata = {
  title: "Sign Up - Shipyard Client Portal",
  description: "Create a new Shipyard account",
};

export default function SignupPage() {
  return <SignupForm />;
}
